
const closeAccountButton = document.getElementById("closeAccountBtn");

closeAccountButton.addEventListener("click", () => {
  const confirmation = confirm("Are you sure you want to close this account?");

  if (confirmation) {
  }
});